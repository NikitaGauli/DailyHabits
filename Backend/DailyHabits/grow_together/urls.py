"""
Grow Together — URL Configuration
===================================

This module is intentionally empty. All endpoints are registered via the
centralised ``DefaultRouter`` in ``DailyHabits.api_router``.

Keeping this file present satisfies the Django app convention and allows
future addition of non-ViewSet URL patterns if needed.
"""

from django.urls import path

urlpatterns: list = []
